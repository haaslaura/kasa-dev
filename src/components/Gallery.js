function Gallery({ galleryArray }) {

    // console.log(galleryArray); n'est pas un tableau comme devrait


    return (
        <div>
            Ici la gallery !
        </div>
    );
}

export default Gallery;